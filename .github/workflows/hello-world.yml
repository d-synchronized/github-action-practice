name: Hello-World

on: push
jobs:
  hello-world-job:
    runs-on: ubuntu-latest
    steps:
      - name: checkout out code
        uses: actions/checkout@v6
      - run: echo "${cat hello_world.txt}"
